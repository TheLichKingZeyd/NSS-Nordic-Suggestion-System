@model IEnumerable<NordicDoorSuggestionSystem.Entities.Suggestion>

@{
    ViewBag.Title = "Mine Forslag § NSS";
    Layout = "~/Views/Shared/_BaseLayout.cshtml";

}

<div class="cardContainer" >
<div id="notificationBox" class="notificationBox" style="margin-top:60vh;">
    <div class="notificationTitle">
        <h1>Notifikasjoner</h1>
    </div>
    <div class="notificationContainer">
        <div class="notification">
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
            <label>Notifikasjon</label>
        </div>
    </div>
    <button id="closeNotifiBtn" type="button"><h3>Lukk</h3></button>
</div>

<script type="text/javascript">
    //get the modal and opacity view id

    var notifiModal = document.getElementById("notificationBox");
    var childHeader = document.getElementById("childHeader");
    var childFooter = document.getElementById("childFooter");
    //get the button id
    var notiBtn = document.getElementById("notificationBtn");
    var closeNotifiBtn = document.getElementById("closeNotifiBtn");

    //function for notifications view
    notiBtn.onclick = function () {
        notifiModal.style.display = "flex";
        childHeader.style.opacity = "50%";
        childFooter.style.opacity = "50%";
    }

    closeNotifiBtn.onclick = function () {
        notifiModal.style.display = "none";
        childHeader.style.opacity = "100%";
        childFooter.style.opacity = "100%";
    }</script>

</div>

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-controller="Suggestion" asp-action="MySuggestions">
    <p>
        Title: <input type="text" name="SearchString" />
        <input type="submit" value="Filter" />
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Problem)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.Solution)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.Goal)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.ResponsibleEmployee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Deadline)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TeamID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Problem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Solution)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Goal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResponsibleEmployee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Deadline)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TeamID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SuggestionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SuggestionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SuggestionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
